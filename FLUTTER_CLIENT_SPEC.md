# مواصفة تطبيق العميل Flutter - التخفيض الصح

## الحالة
تم فحص النسخة المبنية الفعلية في فرع server-authoritative-admin-2026-09، مع التركيز على index.html ثم bundle العامل index-Co2L1R-b.js وجسر takhfid-bridge.js والصور المرفقة.

## شاشات العميل الأساسية
1. متجر: الهيدر، البحث، المفضلة، الإشعارات، البحث البصري، البانرات، القسائم، دوائر الأقسام، تبويبات الفرز، شبكة المنتجات، السحب للتحديث.
2. الفئات: الأقسام الرئيسية، الفئات الفرعية، بطاقات الإطلالة، فتح المنتج والعودة للمتجر.
3. ترندات: الحملات، الهاشتجات، تصفية الترند، فتح المنتج، المفضلة، السلة.
4. حقيبة التسوق: تحديد الأصناف، الكمية، حذف، مسح، الإجمالي، متابعة الطلب، محادثة العميل.
5. أنا: الحساب، الدفع، الموقع، اللغة، العملة، المفضلة، الطلبات، الهدايا والكوبونات، الدعم، تسجيل الدخول والخروج.

## طبقة النوافذ والتفرعات
- تفاصيل الصنف
- البحث
- المفضلة
- الإشعارات
- البحث البصري
- حالة الطلب
- محادثة العميل
- شاشة العرض المرتبطة بالبانر
- تصفية الفئة/الفئة الفرعية/الإطلالة
- دليل المقاسات
- معرض الصور والتكبير
- خيارات الخصم والقسائم
- الهدايا والبطاقات
- المحفظة
- إعدادات الحساب

## التسلسل المرئي
الشاشة الرئيسية في الصور المرفقة تبدأ بشريط علوي صغير، ثم بانر كامل العرض، ثم شرائح العروض، ثم صفوف الصور الدائرية، ثم صف الترتيب، ثم شبكة منتجات ثنائية الأعمدة ببطاقات عمودية ونسب صور متناوبة. شريط التنقل السفلي ثابت بخمسة عناصر: متجر، الفئات، ترندات، حقيبة التسوق، أنا.

## التصميم
- RTL
- أبيض كلون أساس للمتجر
- أسود للعناصر الرئيسية والأزرار المؤكدة
- rose/red للخصومات والحالات
- slate للنصوص الثانوية
- عناصر صغيرة ومدمجة
- بطاقات منتجات متناوبة
- صور بانر full-bleed مع طبقة شفافة
- خط عربي قريب من Cairo/Tajawal
- لا توجد لوحة إدارة داخل تطبيق العميل

## الأحداث
- الضغط على المنتج يفتح التفاصيل
- القلب يبدل المفضلة
- زر السلة يضيف الكمية الافتراضية
- اختيار اللون والمقاس يغير مواصفات السلة
- الضغط على البانر يطبق targetType القادم من الخادم
- الفرز: من أجلك، جديد، خصومات، الأكثر مبيعاً
- سحب الصفحة من الأعلى يحدث refresh
- السحب داخل البانر يغير الشريحة
- تغيير المحافظة/العملة يغير العرض المحلي للأسعار
- متابعة الطلب ترسل بيانات السلة للخادم
- الدخول: رقم الهاتف ثم OTP ثم إكمال الملف

## API الخادم
القاعدة:
https://whats.alattab.site/takhfid/api/v4

قراءة:
GET /products?limit=200
GET /content
GET /categories
GET /pricing
GET /orders
GET /auth/me

مصادقة:
POST /auth/send-otp
POST /auth/verify-otp
POST /auth/complete-profile
POST /auth/logout

طلب:
POST /orders

تحديث حالة الطلب موجود في طبقة الجسر كـ PATCH /orders/{id}/status لكنه ليس حدثا يجب إعطاؤه لعميل عادي دون صلاحية.

## التخزين المحلي
النسخة الحالية تستخدم LocalStorage للسلة والمفضلة والملف الشخصي وبعض الكاش والإعدادات. Flutter سيستخدم SharedPreferences لنفس الاستخدام الخفيف، مع إبقاء الكتالوج والمحتوى والطلب والحساب معتمدين على الخادم.

## قاعدة التنفيذ
هذا الفرع مستقل من ناحية شجرة الملفات: لا ننقل ملفات React أو Android أو بيانات seeds القديمة إلى مشروع Flutter. سيتم بناء واجهة العميل من الصفر، ثم مطابقة التفاصيل البصرية تدريجيا مع الصور والنسخة العاملة.
